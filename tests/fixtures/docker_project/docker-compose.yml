version: "3.8"
services:
  web:
    build: .
    ports:
      - "8080:8000"
  postgres:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: secret
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
